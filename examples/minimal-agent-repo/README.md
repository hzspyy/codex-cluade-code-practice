# Minimal Agent Repository

This fixture shows the smallest repository shape that Agent Workbench can
bootstrap and audit.

To try it manually:

```bash
git init
python -m agent_workbench init .
python -m agent_workbench audit .
```
