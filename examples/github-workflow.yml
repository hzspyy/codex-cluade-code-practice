name: Agent Workbench

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read
  security-events: write

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false
      - uses: hzspyy/codex-cluade-code-practice@main
        with:
          format: sarif
          output: agent-workbench.sarif
          baseline: agent-workbench-baseline.json
          changed-lines: "true"
          base-ref: origin/main
          summary-output: agent-workbench-summary.md
      - uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: agent-workbench.sarif
      - uses: actions/upload-artifact@v5
        if: always()
        with:
          name: agent-workbench-summary
          path: agent-workbench-summary.md
